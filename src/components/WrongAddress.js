import React from 'react';

export default function WrongAddress() {
    return (
        <div>Sorry, the address you searched for does not exist, please try again or click on header to return to homepage.</div>
    )
}